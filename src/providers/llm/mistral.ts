import type { LanguageModel } from 'ai';

export function createMistralProvider(apiKey: string, modelId: string): LanguageModel {
  void apiKey;
  void modelId;
  return null as unknown as LanguageModel;
}
